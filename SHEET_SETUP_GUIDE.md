# Google Sheet সেটআপ গাইড — GBDS অভিযোগ ও নিরাপত্তা পোর্টাল

এই গাইডটি ধাপে ধাপে দেখাবে কীভাবে Google Sheets + Google Apps Script ব্যবহার করে ব্যাকএন্ড সেট আপ করবেন। সময় লাগবে প্রায় ১০-১৫ মিনিট।

## ধাপ ১: নতুন Google Sheet তৈরি করুন

1. [sheets.google.com](https://sheets.google.com) এ যান।
2. **Blank spreadsheet** ক্লিক করে নতুন একটি Sheet তৈরি করুন।
3. উপরে বাম পাশে নাম পরিবর্তন করুন: `GBDS Complaint Portal - Database`
4. আলাদাভাবে কোনো শীট/কলাম তৈরি করার দরকার নেই — স্ক্রিপ্ট প্রথমবার চালানোর সময় স্বয়ংক্রিয়ভাবে `Complaints` নামে একটি শীট এবং সব হেডার তৈরি করে দেবে।

## ধাপ ২: Apps Script যুক্ত করুন

1. Sheet-এর মধ্যে উপরে মেনু থেকে **Extensions → Apps Script** এ ক্লিক করুন।
2. একটি নতুন ট্যাব খুলবে, যেখানে ডিফল্ট একটি `Code.gs` ফাইল থাকবে — সেখানে থাকা সব কোড **মুছে ফেলুন**।
3. এই প্রজেক্টের `Code.gs` ফাইলের সম্পূর্ণ কোড কপি করে সেখানে **পেস্ট করুন**।
4. উপরে প্রজেক্টের নাম দিন: `GBDS Complaint Portal Backend` (ঘড়ির আইকনের পাশে "Untitled project" লেখায় ক্লিক করে)।
5. **Save** (Ctrl+S / Cmd+S) চাপুন।

## ধাপ ৩: প্রয়োজনীয় Permission অনুমোদন করুন

1. Apps Script এডিটরে উপরে থাকা ফাংশন ড্রপডাউন থেকে `testSubmission` নির্বাচন করুন এবং **Run** বাটনে ক্লিক করুন।
2. প্রথমবার চালানোর সময় Google একটি Authorization পপআপ দেখাবে:
   - **Review permissions** ক্লিক করুন।
   - আপনার Google অ্যাকাউন্ট (যেটি দিয়ে Sheet তৈরি করেছেন) নির্বাচন করুন।
   - "Google hasn't verified this app" মেসেজ দেখালে **Advanced → Go to GBDS Complaint Portal Backend (unsafe)** ক্লিক করুন। এটি স্বাভাবিক — কারণ এটি আপনার নিজের তৈরি স্ক্রিপ্ট, তাই "unsafe" শুধু Google-এর একটি সতর্কবার্তা মাত্র।
   - **Allow** ক্লিক করুন।
3. স্ক্রিপ্ট চলার পর `Execution log` এ একটি সফল রেসপন্স দেখতে পাবেন এবং Sheet-এ ফিরে গেলে `Complaints` নামে একটি নতুন শীট এবং একটি টেস্ট রো দেখতে পাবেন।
4. পরীক্ষার এই রো-টি (রো নম্বর ২) চাইলে মুছে ফেলতে পারেন।

## ধাপ ৪: Evidence ফোল্ডার সম্পর্কে জানুন

- যখনই কেউ প্রমাণ (ছবি/ভিডিও/অডিও/PDF) সংযুক্ত করে অভিযোগ জমা দেবে, স্ক্রিপ্টটি স্বয়ংক্রিয়ভাবে আপনার Google Drive-এ **"GBDS Complaint Evidence"** নামে একটি ফোল্ডার তৈরি করবে।
- প্রতিটি অভিযোগের জন্য এর ভেতরে Complaint ID অনুযায়ী একটি সাব-ফোল্ডার তৈরি হবে (যেমনঃ `GBDS-2026-0001`), এবং সেখানে সব ফাইল জমা হবে।
- এই ফাইলগুলোর লিংক Sheet-এর "প্রমাণের লিংক" কলামে এবং ইমেইলেও পাওয়া যাবে।

## ধাপ ৫: পরবর্তী ধাপ

Apps Script এখন ব্যবহারের জন্য প্রস্তুত। এরপর **DEPLOYMENT_GUIDE.md** অনুসরণ করে এটিকে Web App হিসেবে ডিপ্লয় করুন এবং সেই URL-টি `script.js` ফাইলের `CONFIG.GAS_WEB_APP_URL` এ বসান।

## সমস্যা সমাধান (Troubleshooting)

| সমস্যা | সমাধান |
|---|---|
| Sheet-এ কোনো ডেটা আসছে না | Apps Script → **Executions** ট্যাবে গিয়ে এরর দেখুন |
| ইমেইল পাচ্ছেন না | Gmail-এর Spam ফোল্ডার চেক করুন; MailApp-এর দৈনিক কোটা (সাধারণ Gmail অ্যাকাউন্টে ~১০০টি) অতিক্রম হয়েছে কিনা দেখুন |
| Permission এরর | স্ক্রিপ্টটি যে Google অ্যাকাউন্টে অনুমোদন দিয়েছেন, ঠিক সেই অ্যাকাউন্ট দিয়েই Sheet-এর মালিকানা আছে কিনা নিশ্চিত করুন |
